# Required — Taostats API key (get from https://docs.taostats.io)
TAOSTATS_API_KEY=your-taostats-api-key-here

# Required — DRAIN provider wallet private key (for claiming payments)
PROVIDER_PRIVATE_KEY=0xyour_private_key_here

# Optional — Taostats API base URL (default: https://api.taostats.io)
# TAOSTATS_API_URL=https://api.taostats.io

# Optional — Polygon RPC for reliable claiming (highly recommended)
# POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY

# Optional — Price per request in USDC (default: 0.005 = half a cent)
# PRICE_PER_REQUEST_USDC=0.005

# Optional — Chain ID: 137 = Polygon mainnet, 80002 = Amoy testnet (default: 137)
# CHAIN_ID=137

# Optional — Server port (default: 3000)
# PORT=3000

# Optional — Auto-claim interval in minutes (default: 10)
# AUTO_CLAIM_INTERVAL_MINUTES=10

# Optional — Auto-claim buffer in seconds (default: 3600 = 1h before expiry)
# AUTO_CLAIM_BUFFER_SECONDS=3600
